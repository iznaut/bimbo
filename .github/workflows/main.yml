name: build and deploy

permissions:
  contents: write
  
on:
  push:
    branches:
      - main
    paths:
      - 'version'

env:
  ITCH_USERNAME: iznaut
  ITCH_GAME_ID: bimbo

defaults:
  run:
    shell: bash

jobs:
  build:
    runs-on: ${{ matrix.os.runner }}
    strategy:
      matrix:
        sku: ['', '+']
        os: [
          { runner: 'macos-latest', channel: 'macos' },
          { runner: 'macos-15-intel', channel: 'macos-intel' },
          { runner: 'ubuntu-latest', channel: 'linux' },
          { runner: 'windows-latest', channel: 'windows' }
        ]

    steps:
      - uses: actions/checkout@v6.0.3
      
      - name: Install Node.js
        uses: actions/setup-node@v6.4.0
        with:
          node-version: 22
      - name: npm install
        uses: bahmutov/npm-install@v1
      - name: nonplus
        if: ${{ matrix.sku == '' }}
        run: mv deploy-nonplus.js deploy.js
      - name: electron packager
        run: npx @electron/packager . --extra-resource project-starters/ --ignore project-starters/ --out build/ --overwrite
      - uses: actions/upload-artifact@v7.0.1
        with:
          name: ${{ matrix.os.channel }}${{ matrix.sku }}
          path: build
  deploy:
    name: Upload to Itch
    needs: build
    runs-on: ubuntu-latest
    strategy:
      fail-fast: true
      matrix:
        sku: ['', '+']
        channel:
          - macos
          - macos-intel
          - linux
          - windows
    steps:
      - uses: actions/checkout@v6.0.3

      - uses: actions/download-artifact@v8
        with:
          name: ${{ matrix.channel }}${{ matrix.sku }}
          path: build/${{ matrix.channel }}${{ matrix.sku }}
      - uses: KikimoraGames/itch-publish@v0.0.3
        with:
          butlerApiKey: ${{secrets.BUTLER_API_KEY}}
          gameData: ./build/${{ matrix.channel }}${{ matrix.sku }}
          itchUsername: ${{env.ITCH_USERNAME}}
          itchGameId: ${{ env.ITCH_GAME_ID }}
          buildChannel: ${{ matrix.channel }}${{ matrix.sku }}
          buildNumberFile: ./version
  notify:
    name: send update notification to Discord
    runs-on: ubuntu-latest
    needs: deploy
    steps:
      - uses: actions/checkout@v6.0.3

      - name: get version
        run: echo "version=$(cat ./version)" >> $GITHUB_ENV
      - name: send discord message
        uses: appleboy/discord-action@v1.2.0
        with:
          webhook_url: ${{ secrets.DISCORD_WEBHOOK }}
          message: "${{ env.version }} now available on itch.io"