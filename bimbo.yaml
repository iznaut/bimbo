site:
  title: bimpo blog
  description: dear diary...i'm a blogger now :)
  authorName: xXx_sexygurl69_xXx
  authorUrl: https://penisland.net/
  dateFormat: YYYY-MM-DD

contentDefaults: # these get applied to any .md files with no `~default.yml` file in their folder
  title: cool untitled page # you can set all of these items per .md file. these are fallbacks
  template: default.html # be sure to include `{{{content}}}` in your template if you want stuff to appear
  draft: false # draft content is ignored on build and won't be published
