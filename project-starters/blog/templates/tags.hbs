<!DOCTYPE html>
<html>
    {{> head}}

    <body>
        <div id="container">
            {{> navbar}}
            <div id="content">
                <h1>{{ title }}</h1>
                {{{ _content }}}
                {{> post-list-tagged}}
            </div>
            {{> footer}}
        </div>
    </body>

</html>
