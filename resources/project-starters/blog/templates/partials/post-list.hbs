<div id="postlistdiv">
    <ul>
        {{#each $allPosts}}
            <li>
                <a href="{{ this._relativeUrl }}">{{ this.date }} » {{ this.title }}</a>
            </li>
        {{/each}}
    </ul>
</div>
